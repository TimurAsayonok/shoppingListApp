import {
  StyleSheet,
} from 'react-native';

const ProgressLineStyles = StyleSheet.create({
  container: {
    width: '100%',
    height: 5,
    borderRadius: 2,
    borderWidth: 1
  },
  body: {
    height: '100%',
  },
});

export {
  ProgressLineStyles
};